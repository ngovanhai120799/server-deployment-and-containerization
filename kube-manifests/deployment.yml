apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-jwt-app
  labels:
    app: simple-jwt-app
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: simple-jwt-app
  template:
    metadata:
      labels:
        app: simple-jwt-app
    spec:
      containers:
        - name: simple-jwt-app
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080